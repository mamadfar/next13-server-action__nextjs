import React from 'react';

const Header = () => {
    return (
        <header className="text-center mb-4 py-3 border-b">
            <h1 className="font-bold text-3xl">NextJS 13.4.5 Server Actions + MongoDB(mongoose)</h1>
        </header>
    );
};

export default Header;
